{
  "name": "devops-pipeline-demo",
  "version": "1.0.0",
  "description": "Projeto de exemplo para aula de DevOps: CI, Continuous Delivery e Continuous Deployment.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "lint": "eslint .",
    "test": "jest --coverage",
    "build": "node scripts/build.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^9.10.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
